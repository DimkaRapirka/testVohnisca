# Новые функции Vohnisca v2.1

## Обзор добавленного функционала

### 1. ✅ Система приглашений игроков

**Модель данных:**
```typescript
Invitation {
  id: string
  companyId: string
  invitedEmail?: string      // Email приглашенного
  invitedUserId?: string     // ID если уже зарегистрирован
  inviteCode: string         // Уникальный код для ссылки
  role: 'player' | 'master'
  status: 'pending' | 'accepted' | 'declined' | 'expired'
  message?: string           // Сообщение от мастера
  expiresAt?: Date
}
```

**Функционал для мастера:**
- Создание приглашений по email или универсальной ссылкой
- Просмотр списка приглашений и их статусов
- Копирование ссылки-приглашения

**Функционал для игрока:**
- Страница `/invite/[code]` для принятия приглашения
- Возможность принять или отклонить
- Автоматическое добавление в компанию при принятии

**API:**
- `GET /api/companies/[id]/invitations` - список приглашений
- `POST /api/companies/[id]/invitations` - создать приглашение
- `GET /api/invitations/[code]` - информация о приглашении
- `POST /api/invitations/[code]` - принять/отклонить

---

### 2. ✅ Расширенная система персонажей

**Обновленная модель Character:**
```typescript
Character {
  // Базовые поля
  name, class, race, level
  background, alignment, avatarUrl
  
  // Характеристики D&D
  strength, dexterity, constitution
  intelligence, wisdom, charisma
  
  // Боевые параметры
  hp, maxHp, ac
  
  // Прочее
  gold, experience, notes
  isActive: boolean  // Активен ли в кампании
  
  // Связи
  userId, companyId (опционально)
  inventory: InventoryItem[]
}
```

**API:**
- `GET /api/characters` - все персонажи пользователя
- `POST /api/characters` - создать персонажа
- `GET /api/characters?companyId=xxx` - персонажи в кампании

---

### 3. ✅ Выбор персонажа для кампании

**Механика:**
- Игрок может привязать персонажа к кампании
- Один активный персонаж на кампанию
- Возможность смены персонажа

**Модель CompanyPlayer обновлена:**
```typescript
CompanyPlayer {
  companyId: string
  userId: string
  activeCharacterId?: string  // Активный персонаж
  joinedAt: Date
}
```

**API:**
- `POST /api/companies/[id]/party` - установить активного персонажа

---

### 4. ✅ Панель партии для мастера

**Компонент `PartyPanel`:**
- Отображает всех активных персонажей в кампании
- Показывает: имя, класс, уровень, HP, AC, количество предметов
- Визуальная полоска HP с цветовой индикацией
- Для мастера: дополнительно золото и характеристики

**Информация в панели:**
```
[Аватар] Имя персонажа | Раса Класс | Ур.X | HP: XX/XX | AC: XX | 📦 X | 💰 X
         Игрок: Имя игрока
```

**API:**
- `GET /api/companies/[id]/party` - получить партию

---

### 5. ✅ Хроника кампании (Session Logs)

**Модель SessionLog:**
```typescript
SessionLog {
  id: string
  companyId: string
  title: string              // "Сессия 4: Осада форта"
  sessionNumber: number      // Автоинкремент
  summary: string            // Краткое содержание
  detailedNotes?: string     // Заметки мастера (скрыты)
  playedAt: Date             // Когда играли
  duration?: number          // Длительность в минутах
  isPublished: boolean       // Видно ли игрокам
  participants: SessionParticipant[]
}

SessionParticipant {
  characterName: string
  wasPresent: boolean
  notes?: string
}
```

**Функционал:**
- Страница `/companies/[id]/chronicle` - хроника кампании
- Создание записей о сессиях
- Публичное краткое содержание для игроков
- Скрытые детальные заметки для мастера
- Список участников сессии

**API:**
- `GET /api/companies/[id]/sessions` - список сессий
- `POST /api/companies/[id]/sessions` - создать запись

---

## Новые компоненты

### UI компоненты:
- `PartyPanel` - панель партии
- `PartyPanelCompact` - компактная версия для sidebar
- `InvitationManager` - управление приглашениями
- `SessionChronicle` - хроника сессий
- `RoleBadge` - бейдж роли (Мастер/Игрок)
- `VisibilityToggle` - переключатель видимости
- `MasterOnly` - контейнер для секретного контента

### Хуки:
- `useUserRole` - определение роли пользователя в компании

---

## Новые страницы

- `/invite/[code]` - принятие приглашения
- `/companies/[id]/chronicle` - хроника кампании
- `/companies/[id]/campaign` - главная страница кампании
- `/companies/[id]/parts/[partId]` - страница главы

---

## Обновленные страницы

### `/companies/[id]` (страница компании):
- Добавлен бейдж роли
- Кнопки "Кампания" и "Хроника"
- Панель партии
- Управление приглашениями (для мастера)

---

## Структура файлов

```
src/
├── app/
│   ├── api/
│   │   ├── characters/route.ts           # API персонажей
│   │   ├── invitations/[code]/route.ts   # API приглашений
│   │   └── companies/[id]/
│   │       ├── invitations/route.ts      # Приглашения компании
│   │       ├── sessions/route.ts         # Хроника сессий
│   │       └── party/route.ts            # Партия
│   ├── invite/[code]/page.tsx            # Страница приглашения
│   └── companies/[id]/
│       └── chronicle/page.tsx            # Хроника
├── components/
│   ├── party-panel.tsx                   # Панель партии
│   ├── invitation-manager.tsx            # Управление приглашениями
│   ├── session-chronicle.tsx             # Хроника сессий
│   ├── role-badge.tsx                    # Бейдж роли
│   ├── visibility-toggle.tsx             # Переключатель видимости
│   └── master-only.tsx                   # Секретный контент
└── hooks/
    └── useUserRole.ts                    # Хук роли
```

---

## Использование

### Пригласить игрока:
1. Откройте страницу компании
2. В разделе "Приглашения" нажмите "Пригласить"
3. Введите email или оставьте пустым для ссылки
4. Скопируйте ссылку и отправьте игроку

### Принять приглашение:
1. Перейдите по ссылке приглашения
2. Войдите или зарегистрируйтесь
3. Нажмите "Присоединиться"

### Добавить запись в хронику:
1. Откройте страницу компании
2. Нажмите "Хроника"
3. Нажмите "Добавить сессию"
4. Заполните информацию о прошедшей игре

### Просмотреть партию:
1. Откройте страницу компании
2. Панель "Партия" показывает всех персонажей
3. Мастер видит расширенную информацию

---

## Безопасность

- Все API проверяют авторизацию
- Приглашения имеют срок действия
- Секретные заметки фильтруются на сервере
- Только мастер может создавать приглашения и записи хроники

---

**Версия**: 2.1.0
**Статус**: ✅ Реализовано
