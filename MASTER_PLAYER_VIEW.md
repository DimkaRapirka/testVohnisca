# Система Master View / Player View

## Архитектура двух режимов отображения

Vohnisca реализует полноценную систему разделения контента между мастером и игроками.

## Ключевые принципы

### 1. Серверная фильтрация
Вся логика приватности обрабатывается на сервере. Игрок физически не может получить скрытый контент через API.

### 2. Поля приватности
Каждая сущность имеет:
- `isPublished` - опубликовано ли для игроков
- `masterNotes` / `masterContent` - секретный контент мастера
- `publicDesc` / `publicContent` - публичный контент для игроков

### 3. Роли
- **Master** - видит всё, может редактировать, управляет видимостью
- **Player** - видит только опубликованный контент

## Структура кампании

```
Company (Кампания)
├── Parts (Главы)
│   ├── Locations (Локации)
│   │   ├── NPCs
│   │   ├── Creatures (Существа)
│   │   └── LootItems (Лут)
│   └── Events (События)
└── WikiEntries (Вики)
```

## Новые сущности

### Part (Глава кампании)
```typescript
{
  id: string;
  name: string;
  description: string;      // Краткое описание
  masterContent: string;    // Секреты мастера
  publicContent: string;    // Публичный контент
  isPublished: boolean;     // Видна ли игрокам
  order: number;            // Порядок
  locations: Location[];
  events: Event[];
}
```

### Location (Локация)
```typescript
{
  id: string;
  name: string;
  description: string;
  masterNotes: string;      // Секретные заметки
  publicDesc: string;       // Публичное описание
  imageUrl: string;
  mapUrl: string;
  isPublished: boolean;
  npcs: Npc[];
  creatures: LocationCreature[];
  lootItems: LootItem[];
}
```

### NPC
```typescript
{
  id: string;
  name: string;
  description: string;
  masterNotes: string;      // Секретная информация
  publicDesc: string;       // Что знают игроки
  race: string;
  occupation: string;
  isPublished: boolean;
  isAlive: boolean;
}
```

### Event (Событие)
```typescript
{
  id: string;
  name: string;
  description: string;
  masterNotes: string;
  publicDesc: string;
  eventType: 'story' | 'combat' | 'social' | 'discovery';
  isPublished: boolean;
  isCompleted: boolean;
}
```

### WikiEntry (Вики)
```typescript
{
  id: string;
  title: string;
  content: string;          // Публичный контент
  masterContent: string;    // Секретный контент
  category: 'lore' | 'faction' | 'mythology' | 'item' | 'other';
  isPublished: boolean;
}
```

## API Endpoints

### Части кампании
- `GET /api/companies/[id]/parts` - список частей (фильтруется по роли)
- `POST /api/companies/[id]/parts` - создать часть (только мастер)

### Локации
- `GET /api/parts/[id]/locations` - список локаций (фильтруется по роли)
- `POST /api/parts/[id]/locations` - создать локацию (только мастер)

### Управление видимостью
- `POST /api/visibility` - переключить видимость элемента (только мастер)

## Компоненты

### VisibilityToggle
Кнопка переключения видимости элемента для мастера.

```tsx
<VisibilityToggle
  entityType="part"
  entityId={part.id}
  isPublished={part.isPublished}
/>
```

### MasterOnly
Контейнер для контента, видимого только мастеру.

```tsx
<MasterOnly isMaster={isMaster} label="Секреты">
  <p>{secretContent}</p>
</MasterOnly>
```

### HiddenContentIndicator
Индикатор скрытого контента.

```tsx
<HiddenContentIndicator isPublished={false} />
// Показывает: "Скрыто от игроков"
```

### RoleBadge
Бейдж с ролью пользователя.

```tsx
<RoleBadge role="master" />
// Показывает: 👑 Мастер
```

## Хуки

### useUserRole
Определяет роль пользователя в компании.

```tsx
const { role, isMaster, isPlayer, userId } = useUserRole(masterId, playerIds);
```

## Фильтрация на сервере

### Пример фильтрации частей для игрока

```typescript
// API: GET /api/companies/[id]/parts

if (!userIsMaster) {
  const filteredParts = parts
    .filter((part) => part.isPublished)  // Только опубликованные
    .map((part) => {
      const { masterContent, ...publicPart } = part;  // Убираем секреты
      return {
        ...publicPart,
        locations: part.locations
          .filter((loc) => loc.isPublished)
          .map(({ masterNotes, ...loc }) => loc),
      };
    });
  return filteredParts;
}
```

## UX для мастера

### Создание контента
1. Мастер создает главу/локацию/NPC
2. Заполняет публичный и секретный контент отдельно
3. Решает, опубликовать сразу или позже

### Управление видимостью
1. Каждый элемент имеет кнопку "Видно игрокам" / "Скрыто"
2. Мастер может открывать контент постепенно
3. Скрытые элементы отображаются с пунктирной рамкой

### Визуальные индикаторы
- 🟢 Зеленая рамка - публичный контент
- 🟡 Золотая рамка - секретный контент мастера
- ⚫ Пунктирная рамка - скрытый элемент

## UX для игрока

### Просмотр контента
1. Игрок видит только опубликованные главы
2. Внутри глав - только опубликованные локации
3. В локациях - только опубликованные NPC и предметы

### Ощущение приключения
1. Контент открывается постепенно
2. Нет спойлеров - игрок не знает, что скрыто
3. История разворачивается как настоящее приключение

## Страницы

### /companies/[id]/campaign
Главная страница кампании с главами.

### /companies/[id]/parts/[partId]
Страница главы с локациями и событиями.

### /companies/[id]/parts/[partId]/locations/[locationId]
Страница локации с NPC, существами и лутом.

### /companies/[id]/wiki
Вики кампании (лор, фракции, мифология).

## Безопасность

1. **Проверка роли на сервере** - каждый API endpoint проверяет права
2. **Фильтрация данных** - игрок получает только публичные поля
3. **Проверка владельца** - только мастер может изменять видимость
4. **Нет утечек** - секретный контент не попадает в ответ API для игрока

## Дальнейшее развитие

### Планируется
- [ ] Частичная видимость (для отдельных игроков)
- [ ] История изменений видимости
- [ ] Уведомления игрокам о новом контенте
- [ ] Комментарии к элементам
- [ ] Связи между элементами (NPC ↔ Локация ↔ Событие)

### Улучшения UX
- [ ] Drag & drop для порядка элементов
- [ ] Массовое управление видимостью
- [ ] Предпросмотр "глазами игрока"
- [ ] Шаблоны для быстрого создания

---

**Статус**: ✅ Базовая реализация завершена
**Версия**: 2.0.0
